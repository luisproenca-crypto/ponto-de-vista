/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // O projeto usa apenas recursos nativos do Next.js — nenhuma configuração
  // extra é necessária para o deploy na Vercel.
};

export default nextConfig;
